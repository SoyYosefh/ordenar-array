// 4. Crea un arreglo de números desordenados y luego 
// ordénalo de menor a mayor utilizando algún algoritmo 
// de ordenamiento (p. ej., el método sort()).

const numeros = [5, 3, 8, 1, 2, 9, 4, 7, 6];
console.log(`Números desordenados: ${numeros}`);
const numerosOrdenados = numeros.sort();
console.log(`Números ordenados: ${numerosOrdenados}`);